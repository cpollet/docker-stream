name: my stream
steps:
  - step:
    name: echo 1
    image: alpine
    input: .
    output: .
    command:
      - echo a; sleep 3; echo b; sleep 3; echo c
    environment:
      - TARGET=world
  - step:
    name: echo 2
    image: alpine
    input: .
    output: .
    command:
      - sh
      - -c
      - echo Hello, $TARGET
    environment:
      - TARGET=world
