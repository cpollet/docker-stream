name: my stream
steps:
  - step:
    name: echo 1
    image: alpine
    input: .
    output: .
    command:
      - sh
      - -c
      - sleep 3
    environment:
      - TARGET=world
  - step:
    name: echo 2
    image: alpine
    input: .
    output: .
    command:
      - sh
      - -c
      - echo Hello, $TARGET
    environment:
      - TARGET=again